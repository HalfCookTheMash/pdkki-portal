/** @type {import('next').NextConfig} */
const nextConfig = {
    // Anda dapat menambahkan konfigurasi Next.js lainnya di sini jika diperlukan
    // Misalnya: images: { unoptimized: true } untuk build statis, dll.
}

module.exports = nextConfig